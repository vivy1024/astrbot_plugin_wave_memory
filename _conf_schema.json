{
  "_system_status": {
    "description": "⚡ WaveMemory 系统运行状态说明",
    "type": "string",
    "hint": "本页配置系统基础能力开关和模型选择。核心交互机制：(1) 记忆注入：允许机器人结合历史上下文在聊天中主动提及过去的交往经历；(2) 深度社交认知：无需独立调用LLM，系统根据互动次数和事实三元组自动沉淀出对每个群友的用户画像，实现自然的陪伴调教；(3) 情绪半衰期防刷机制：辱骂等恶意事件3次后自动进入安全冷却期。",
    "obvious_hint": true,
    "default": "仅供说明，无需修改",
    "_readonly": true
  },
  "embedding_provider_id": {
    "description": "向量特征模型 (Embedding) 提供商",
    "type": "string",
    "hint": "填写您在 AstrBot 中创建的 Embedding 提供商完整ID（如 'siliconflow/Qwen3-Embedding'）。向量模型负责对聊天记录进行空间语义转化，使其具备模糊联想能力。",
    "obvious_hint": true,
    "default": "",
    "restart_required": true
  },
  "embedding_dimension": {
    "description": "特征向量维度",
    "type": "int",
    "hint": "必须与所选择的 Embedding 特征模型输出维度完全一致。通义千问 Qwen3-Embedding 默认 1024 维，SiliconFlow 或 OpenAI 依模型而定。",
    "default": 1024,
    "restart_required": true
  },
  "backup_max_count": {
    "description": "自动整库备份保留数量",
    "type": "int",
    "hint": "WaveMemory 数据库体积较大，默认只保留最近 1 份自动整库备份；治理前备份应另行验证后再裁剪。",
    "default": 1,
    "restart_required": false
  },
  "tag_llm_provider_id": {
    "description": "自省标签提取大模型 (Tag LLM)",
    "type": "string",
    "hint": "用于对聊天记录分析并提炼标签、事实三元组（Subject-Predicate-Object）的大模型 ID。留空则只用向量搜索，不生成知识图谱、黑话和心智信念。",
    "default": "",
    "_special": "select_provider",
    "restart_required": true
  },
  "llm_fallback_provider_ids": {
    "description": "LLM 回退模型链",
    "hint": "逗号分隔多个 provider ID，按顺序尝试。主模型（Tag LLM）不可用时依次回退。记忆整合、标签提取、黑话、信念、自主学习、自省共用此链。示例：伞云智联/grok-4.5, paidyun/grok-4.5, gemini/gemini-3.5-flash",
    "type": "string",
    "default": "",
    "restart_required": true
  },
  "WebUI_Settings": {
    "description": "管理控制台",
    "hint": "本控制台自身的开关与端口",
    "type": "object",
    "items": {
      "webui_enabled": {
        "description": "启用管理控制台",
        "hint": "关闭后无法访问本控制台及其接口。",
        "type": "bool",
        "default": true,
        "restart_required": true
      },
      "webui_host": {
        "description": "监听网卡地址",
        "hint": "默认 127.0.0.1 仅允许本机访问；若改为 0.0.0.0，必须同时设置高强度访问密码并配置可信网络边界。",
        "type": "string",
        "default": "127.0.0.1",
        "restart_required": true
      },
      "webui_port": {
        "description": "控制台访问端口",
        "hint": "WebUI 端口号，若冲突请修改（默认 9876）",
        "type": "int",
        "default": 9876,
        "restart_required": true
      },
      "webui_password": {
        "description": "访问登录密码",
        "hint": "监听非回环地址时必须设置高强度密码；仅默认 127.0.0.1 本机监听可留空免密。",
        "type": "string",
        "default": ""
      }
    }
  },
  "Runtime_Settings": {
    "description": "整机运行能力",
    "hint": "整机层面的能力边界",
    "type": "object",
    "items": {
      "runtime_mode": {
        "description": "系统运行级别",
        "hint": "full: 激活完整知识图谱、信念与主动记忆注入能力；memory_only: 纯记忆数据库，不启用高级认知提炼；compat_only: 仅作为第三方兼容接口后端，不执行原生主动注入，避免和 SelfLearning / ChatPlus 等同类插件在同一轮聊天中重复注入。",
        "type": "string",
        "default": "full",
        "restart_required": true
      }
    }
  },
  "Compatibility_Settings": {
    "description": "插件兼容模式",
    "hint": "与其他记忆插件共存的方式",
    "type": "object",
    "items": {
      "livingmemory_alias_tools_enabled": {
        "description": "启用 LivingMemory 别名工具映射",
        "hint": "开启后会自动注册 recall_long_term_memory 工具别名，供其它已绑定 LivingMemory 工具名的外部 Agent 直接调用。未安装相关插件时请保持关闭以避工具名冲突。",
        "type": "bool",
        "default": false,
        "restart_required": true
      },
      "compat_only_auto_inject_enabled": {
        "description": "兼容模式下强行启用原生注入",
        "hint": "仅在您确实希望在 compat_only 极简后端级别下，也让本插件在消息流中插入记忆时开启。默认关闭，确保不重复插话。",
        "type": "bool",
        "default": false,
        "restart_required": true
      }
    }
  },
  "Trace_Settings": {
    "description": "观测数据留存",
    "hint": "注入观测台保留多少条运行记录",
    "type": "object",
    "items": {
      "retention_days": {
        "description": "Trace 历史数据保留天数",
        "hint": "超过此时限的注入轨迹将被自动擦除，防止日志文件过度膨胀（默认 14 天）",
        "type": "int",
        "default": 14,
        "restart_required": true
      },
      "max_rows": {
        "description": "Trace 最大保留记录条数",
        "hint": "超过此上限时自动截断旧日志（默认 5000 条）",
        "type": "int",
        "default": 5000,
        "restart_required": true
      },
      "max_preview_chars": {
        "description": "单条消息最大预览字符数",
        "hint": "为了防止重型上下文撑爆 SQLite 页面，系统对记录在观测台的单次消息预览长度做限制（默认 1200 字）",
        "type": "int",
        "default": 1200,
        "restart_required": true
      }
    }
  },
  "Storage_Settings": {
    "description": "记忆存储容量",
    "hint": "数据库能保存多少记忆，以及是否给总量设软上限",
    "type": "object",
    "items": {
      "max_memories": {
        "description": "活跃记忆软上限（条）",
        "hint": "统计 quarantine/noise 以外的活跃行。超过后新 chat 默认只写文本冷层，不再追加 embedding 字节。不自动删除旧数据。",
        "type": "int",
        "default": 100000,
        "restart_required": true
      },
      "canonical_capacity_enabled": {
        "description": "启用记忆总量软上限",
        "hint": "关闭后新记忆会一直尝试带向量入库，只受热索引容量约束。显式关闭不会被默认值覆盖。",
        "type": "bool",
        "default": true,
        "restart_required": true
      },
      "cold_chat_when_over_capacity": {
        "description": "超额时 chat 冷落库",
        "hint": "开启后，库容量已满时普通 chat 写入 vector=NULL，文本与元数据仍保留。core/book_lore 等耐用源不受影响。",
        "type": "bool",
        "default": true,
        "restart_required": true
      },
      "facts_decay_rate": {
        "description": "facts 时间衰减速率",
        "hint": "旧 facts 召回权重随时间衰减的速率（兼容字段）。",
        "type": "string",
        "default": "0.005",
        "restart_required": true
      }
    }
  },
  "Memory_Budget_Settings": {
    "description": "内存占用预算",
    "hint": "常驻内存的总上限，防止占满机器",
    "type": "object",
    "items": {
      "memory_profile": {
        "description": "内存预算档位",
        "hint": "unbounded 保持历史显式容量不变；balanced_3g 面向容器常驻低于约 3GiB；strict_2g 面向低于约 2GiB，会显著降低 BookLore 常驻量并更多依赖冷召回。修改后需重启。",
        "type": "string",
        "default": "unbounded",
        "options": [
          "unbounded",
          "balanced_3g",
          "strict_2g"
        ],
        "restart_required": true
      },
      "memory_budget_mb": {
        "description": "常驻内存预算（MiB）",
        "hint": "仅在选择 balanced_3g / strict_2g 时生效。默认 balanced_3g=2400、strict_2g=1600，留出重建峰值与文件缓存余量。",
        "type": "int",
        "default": 2400,
        "restart_required": true
      },
      "baseline_reserved_mb": {
        "description": "非索引基线预留（MiB）",
        "hint": "为 Python 解释器、SQLite、服务与缓存预留的常驻内存；预算减去该基线后才分配给向量索引。",
        "type": "int",
        "default": 700,
        "restart_required": true
      },
      "rebuild_headroom": {
        "description": "重建峰值预留比例",
        "hint": "0~0.95。索引重建会短时同时持有新旧两份数据，该比例从索引预算中预留出峰值空间，避免稳态贴满预算后在重建时越界。",
        "type": "float",
        "default": 0.35,
        "restart_required": true
      }
    }
  },
  "Memory_Index_Settings": {
    "description": "记忆检索索引",
    "hint": "决定记忆能被搜到多快、多准，以及热冷如何分流",
    "type": "object",
    "items": {
      "hot_max_vectors": {
        "description": "热记忆最大条数",
        "hint": "常驻内存按这个上限预留，而不是按实际条数，因此设得过高会白白占用内存。达到上限后新记忆留在冷层等待重建，不会自动扩容。",
        "type": "int",
        "default": 40000,
        "restart_required": true
      },
      "enforce_scope_hot_quota": {
        "description": "启用单个群的热记忆配额",
        "hint": "默认关闭：记忆会持续进入热索引直到全局上限。开启后才使用下方的单群上限，适合需要严格平均分配的场景。",
        "type": "bool",
        "default": false,
        "restart_required": true
      },
      "per_scope_max_vectors": {
        "description": "单个群的热记忆上限",
        "hint": "仅在上一项开启时生效。关闭时群仍用于访问隔离与检索过滤，但不限制热索引写入。",
        "type": "int",
        "default": 1000,
        "restart_required": true
      },
      "scoped_reserved_vectors": {
        "description": "为正式群保留的热记忆量",
        "hint": "预留给正式群的热记忆条数，剩余容量由历史群与未入选记忆按优先级竞争，可设为 0。",
        "type": "int",
        "default": 10000,
        "restart_required": true
      },
      "chat_hot_days": {
        "description": "普通聊天热窗口（天）",
        "hint": "具有有效 Tag 的普通聊天只在此窗口内参与热索引；超期后仍在冷层，不会被删除或标为 evicted。",
        "type": "int",
        "default": 30,
        "restart_required": true
      },
      "cold_candidate_limit": {
        "description": "单次冷召回候选上限",
        "hint": "Tag/FTS5 缩小后的候选最多数量；主进程只对这个小批量做向量重排。修改后重启生效。",
        "type": "int",
        "default": 128,
        "restart_required": true
      },
      "cold_recall_enabled": {
        "description": "启用标签驱动的冷记忆召回",
        "hint": "关闭后只搜索热记忆，冷层内容不会被召回。显式关闭会保留，不会被默认值覆盖。修改后需重启生效。",
        "type": "bool",
        "default": true,
        "restart_required": true
      },
      "tag_index_max_vectors": {
        "description": "标签索引最大条数",
        "hint": "标签索引的独立上限，与热记忆条数是两回事。超出部分的标签仍保存在数据库中，但不参与快速检索，也不会自动扩容。",
        "type": "int",
        "default": 40000,
        "restart_required": true
      },
      "generation_retention": {
        "description": "索引历史版本保留数量",
        "hint": "每次校验通过后保留几个旧版本。默认 1 只留当前版本，最省内存和磁盘；设为 2 会多留一个可回滚版本，但多占约一份索引体积。最小为 1。",
        "type": "int",
        "default": 1,
        "restart_required": true
      }
    }
  },
  "Inject_Settings": {
    "description": "回复注入",
    "hint": "注入内容的篇幅与时间窗口",
    "type": "object",
    "items": {
      "enable_holyman_persona": {
        "description": "启用 holyman-skills 风格人格包（可选）",
        "hint": "默认关闭。开启后把 holyman-skills 人格包作为风格参考注入，会改变说话方式；与既有 Bot 人格并存而非覆盖。仅作为默认值，通道页的开关覆盖优先。",
        "type": "bool",
        "default": false,
        "restart_required": false
      },
      "timeline_days": {
        "description": "历史时间窗（天，兼容保留）",
        "hint": "独立群事件 timeline 通道已移除；本字段仅为旧配置兼容保留，当前无通道读取。0 表示全历史。",
        "type": "int",
        "default": 0,
        "restart_required": false
      },
      "timeline_decay_half_life_days": {
        "description": "印象时间线指数权重半衰期（天）",
        "hint": "默认 21 天。权重按 2^(-经过天数/半衰期) 计算，只影响印象摘要的时间权重，不删除历史记录、不截断条数、不衰减关系分数。填写非正数或非法值时会回退到默认 21 天。",
        "type": "float",
        "default": 21,
        "restart_required": false
      },
      "soul_timeline_max_items": {
        "description": "Bot 经历日记时间线注入条数",
        "hint": "在 Soul 状态通道中注入当前群经历日记与成长时间线的条数（默认 3 条）。",
        "type": "int",
        "default": 3,
        "restart_required": false
      },
      "orchestrator_active_enabled": {
        "description": "启用注入编排",
        "hint": "关闭后不再由编排器统一调度各注入通道，回退到旧的逐通道拼接路径。除非排查注入顺序问题，否则保持开启。",
        "type": "bool",
        "default": true
      },
      "orchestrator_shadow_enabled": {
        "description": "启用编排影子模式",
        "hint": "开启后编排器只计算不实际注入，用于对比新旧路径的差异。默认与「启用注入编排」相反。",
        "type": "bool",
        "default": false
      },
      "skip_recent_minutes": {
        "description": "跳过最近多少分钟的消息",
        "hint": "这些分钟内的消息不进记忆召回，避免刚说过的话被原样复述（默认 30 分钟）。",
        "type": "int",
        "default": 30
      },
      "facts_max": {
        "description": "事实注入条数上限",
        "hint": "单次回复最多注入几条已审核事实（默认 5 条）；填 0 表示不注入事实。",
        "type": "int",
        "default": 5
      }
    }
  },
  "Query_Settings": {
    "description": "记忆召回",
    "hint": "Bot 回复时自动带上哪些记忆、带多少条",
    "type": "object",
    "items": {
      "enable_auto_inject": {
        "description": "开启自动记忆注入能力",
        "hint": "每次对话大模型前，后台异步检索关联经历并整合进 context。关闭后机器人将失去一切记忆和主动认知能力！",
        "type": "bool",
        "default": true,
        "obvious_hint": true
      },
      "inject_top_k": {
        "description": "单次最多注入几条记忆",
        "hint": "推荐 5 条，可填 1~20 条。设得过大可能撑满大模型的上下文长度并引发幻觉。",
        "type": "int",
        "default": 5
      },
      "min_similarity": {
        "description": "相似度关联召回门限",
        "hint": "语义空间最低匹配分数（0.0 ~ 1.0）。低于此分数的无关琐碎记忆将绝不注入。建议保持在 0.35 黄金值附近。",
        "type": "float",
        "default": 0.35
      },
      "injection_format": {
        "description": "记忆提示词注入模板 (Template)",
        "hint": "定义记忆在大模型 Context 里的承载标签。可用标签占位符：{content} (记忆内容), {sender} (对方昵称), {time} (发生时间)。建议保留这些标签，让机器人能自然使用“你之前说过...”的亲密口吻。",
        "type": "string",
        "default": "<memory from='{sender}' time='{time}'>{content}</memory>"
      },
      "enable_spike_routing": {
        "description": "启用神经星云脉冲传播 (Spike Routing)",
        "hint": "标签神经星云高级拓扑检索能力。当标签覆盖率>20%且图谱构建完成后起效，支持跨实体多跳语义联想；可在「算法实验室」中可视化对比效果。",
        "type": "bool",
        "default": true
      },
      "enable_residual_pyramid": {
        "description": "启用残差多阶金字塔召回 (Residual Pyramid)",
        "hint": "标签神经星云多阶语义残差差分检索能力，提升多重主语、指代模糊场景下的召回精准度；可在「算法实验室」中可视化对比效果。",
        "type": "bool",
        "default": true
      },
      "enable_epa": {
        "description": "启用自省投影距离修正 (EPA)",
        "hint": "根据问题倾向度动态对记忆向量欧氏几何距离进行投影自省缩放；可在「算法实验室」中可视化对比效果。推荐开启。",
        "type": "bool",
        "default": true
      },
      "enable_geodesic_rerank": {
        "description": "启用非欧流形测地线重排 (Geodesic)",
        "hint": "利用标签神经星云的局部拓扑关联结构对向量空间偏离距离进行拉回校正，抑制向量幻觉关联；可在「算法实验室」中可视化对比效果。",
        "type": "bool",
        "default": true
      },
      "enable_shotgun": {
        "description": "启用霰弹枪扫描模式",
        "hint": "支持多角度、碎片化的快速上下文分割并发查询。开启会略微增加 30ms 响应耗时，但在大群活跃吹水场景下体验极佳。",
        "type": "bool",
        "default": false
      }
    }
  },
  "Cross_Group_Settings": {
    "description": "跨群共享",
    "hint": "记忆与画像是否跨群生效，以及授权范围",
    "type": "object",
    "items": {
      "cross_group_enabled": {
        "description": "开启广域跨群记忆共享",
        "hint": "开启后机器人共享同一个大脑知识，在A群聊过的事B群也认识。关闭后每个QQ群的记忆完全沙盒硬隔离，宛若独立Bot。",
        "type": "bool",
        "default": true
      },
      "shared_memory_grants_enabled": {
        "description": "启用共享记忆只读授权",
        "hint": "默认关闭。开启后可按指定授权列表额外召回少量其他群的记忆，只读、不会改动对方数据，也不会复制记忆。与「全量跨群共享」不同：这是精确到条目的授权，每次召回都留痕。",
        "type": "bool",
        "default": false
      }
    }
  },
  "Lifecycle_Settings": {
    "description": "心智与情绪",
    "hint": "Bot 的心情、关切、作息等内在状态",
    "type": "object",
    "items": {
      "enable_mood": {
        "description": "启用生理心境系统 (Bot Mood)",
        "hint": "根据消息密度与情感 tag 的输入速率，动态在 Energetic / Cheerful / Concerned 情绪轨道间滑动跃迁。",
        "type": "bool",
        "default": true
      },
      "mood_duration_hours": {
        "description": "情绪状态自然半衰退天数",
        "hint": "情绪突发后，在没有后续高强度互动刺激下，多少小时后平复并重置为平静温和状态（默认 2.0 小时）",
        "type": "float",
        "default": 2.0
      },
      "mood_msg_threshold": {
        "description": "情绪爆发活跃消息门限",
        "hint": "群友在 30 分钟内消息数超过该值时，自动触发机器人处于活跃开朗心境下（默认 30 条）",
        "type": "int",
        "default": 30
      },
      "positive_emotion_threshold": {
        "description": "CHEERFUL 喜悦触发临界值",
        "hint": "对话中喜悦情感标签占比超过此比例时，情绪自动偏向 CHEERFUL（0.0 ~ 1.0）",
        "type": "float",
        "default": 0.6
      },
      "negative_emotion_threshold": {
        "description": "CONCERNED 悲伤触发临界值",
        "hint": "对话中负面、关切、悲伤情感占比超过此比例时，情绪自动偏向 CONCERNED（0.0 ~ 1.0）",
        "type": "float",
        "default": 0.4
      },
      "relationship_single_delta_cap": {
        "description": "单次互动关系变化极大值",
        "hint": "单次对话中好感度等关系事件波动最大绝对值。防止一次夸奖好感拉满，或者一次失误好感清零（默认 5.0 分）",
        "type": "float",
        "default": 5.0
      },
      "relationship_daily_delta_cap": {
        "description": "每日好感度变化累计上限",
        "hint": "防刷分机制：同一个人一天内累计好感度变化的绝对值上限（默认 15.0 分）。超过上限后当天的互动将保持稳定。",
        "type": "float",
        "default": 15.0
      },
      "relationship_hostility_delta_cap": {
        "description": "单次辱骂敌意增加上限",
        "hint": "单次检测到辱骂或恶意调戏时，敌意/敌对值正向拉升的最大限值（默认 8.0 分）。达到 3 次后自动进入保护性冷却。",
        "type": "float",
        "default": 8.0
      },
      "enable_dream": {
        "description": "启用深夜做梦引擎 (Dreaming)",
        "hint": "后台自动执行：对零散记忆进行三层“时间线涟漪浪潮”重组，并在历史经历中搭建“共振桥梁”发现潜在相关联，形成长期的逻辑事实和心智。推荐开启。",
        "type": "bool",
        "default": true
      },
      "dream_interval_hours": {
        "description": "做梦引擎触发自省间隔",
        "hint": "每隔多少小时执行一次深夜记忆合并（推荐在夜间执行，默认 6.0 小时）",
        "type": "float",
        "default": 6.0
      },
      "dream_recent_seeds": {
        "description": "近期种子记忆数",
        "hint": "做梦自省时近期涟漪抽取的基础种子记忆数量（默认 3 条）",
        "type": "int",
        "default": 3
      },
      "dream_recent_k": {
        "description": "近期记忆联想召回深度",
        "hint": "每个做梦种子在后台模糊关联出的最相关的历史经历条数（默认 5 条）",
        "type": "int",
        "default": 5
      },
      "dream_mid_seeds": {
        "description": "中期经历种子数",
        "type": "int",
        "default": 2
      },
      "dream_mid_k": {
        "description": "中期经历联想召回深度",
        "type": "int",
        "default": 3
      }
    }
  },
  "Message_Filter": {
    "description": "闲聊过滤",
    "hint": "先过滤掉无意义消息，避免污染记忆",
    "type": "object",
    "items": {
      "min_message_length": {
        "description": "记忆录入最小字符长度",
        "hint": "短于此长度的消息（如：'好'、'1'、'@xxx'）会被自动过滤，绝不写进长期记忆，保护大模型不被垃圾数据撑爆（默认 4 字）",
        "type": "int",
        "default": 4
      },
      "max_message_length": {
        "description": "记忆录入最大字符长度",
        "hint": "单条消息记录的最大物理上限，超过此限制的超长篇幅文本将截断后记录（默认 2000 字）",
        "type": "int",
        "default": 2000
      },
      "ignore_bot_messages": {
        "description": "忽略机器人自身回复记忆",
        "hint": "开启后，大模型自己生成的回复话语将不写进记忆。关闭后，机器人自己的话也会写进长期记忆。推荐关闭（即：机器人也需要记住自己说过的话）",
        "type": "bool",
        "default": false
      },
      "group_whitelist": {
        "description": "群记忆功能激活白名单",
        "hint": "仅记录这些群组的消息。格式：逗号分隔的群号。留空则表示对全部群生效。",
        "type": "string",
        "default": ""
      },
      "group_blacklist": {
        "description": "群记忆功能禁止黑名单",
        "hint": "格式：逗号分隔的群号。此名单中的群消息绝不记录。",
        "type": "string",
        "default": ""
      }
    }
  },
  "Tag_Settings": {
    "description": "标签与神经星云 (Tag Graph)",
    "hint": "从聊天中自动提炼标签，以及标签神经星云的图例、类型与展示配置",
    "type": "object",
    "items": {
      "tag_extraction_enabled": {
        "description": "启用标签特征分析",
        "hint": "关闭后不再从聊天提炼标签，标签神经星云、同好社区共现与自省投影修正都会失效。推荐开启。",
        "type": "bool",
        "default": true
      },
      "max_tags_per_message": {
        "description": "单条消息最大标签数",
        "hint": "LLM 每次从聊天片段中分析提炼出的人物、话题、事实等最大标签数（默认 10 个）",
        "type": "int",
        "default": 10
      },
      "tag_batch_size": {
        "description": "标签提炼实时队列批次大小",
        "hint": "当聊天缓冲区积累到多少条有效记忆时，自动触发一次 LLM 标签并行分析队列（默认 5 条）",
        "type": "int",
        "default": 5
      },
      "tag_backfill_batch_size": {
        "description": "后台标签补填任务处理大小",
        "hint": "当管理员在控制台点击“一键提取全量Tag”时，后台每批处理的记忆块大小（默认 50 条），防止撑爆 LLM 并发流",
        "type": "int",
        "default": 50
      },
      "tag_blacklist": {
        "description": "全局标签屏蔽词",
        "hint": "逗号分隔。这些词即使大模型分析提炼出来，也会在入库前被彻底剔除。防止无意义杂词污染图谱。",
        "type": "string",
        "default": ""
      },
      "graph_legend_enabled": {
        "description": "显示神经星云图例",
        "hint": "关闭后标签神经星云左上角不再显示图例与节点计数。星云图谱本身不受影响。",
        "type": "bool",
        "default": true
      },
      "graph_legend_types": {
        "description": "神经星云图例展示类型与顺序",
        "hint": "逗号分隔，按填写顺序展示。可选真实类型：topic（话题）、event（事件）、keyword（关键词）、entity（实体）、fact（事实）、person（人物）、emotion（情绪）、location（地点）、time（时间）、jargon（黑话）、default（其他）。留空表示展示星云中实际出现的全部类型。",
        "type": "string",
        "default": "topic,event,keyword,entity,fact,person,emotion,location,time,jargon,default"
      },
      "graph_legend_show_count": {
        "description": "神经星云图例显示节点数量",
        "hint": "在图例中每个类型后显示该类型的节点个数。",
        "type": "bool",
        "default": true
      }
    }
  },
  "MetaThinking_Settings": {
    "description": "对话规则",
    "hint": "全局发言规则与防骚扰策略",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用 MetaThinking",
        "type": "bool",
        "default": true,
        "restart_required": true
      },
      "default_model": {
        "description": "默认模型",
        "hint": "未在 Bot 配置中指定时使用；留空则使用 tag_llm_provider_id。",
        "type": "string",
        "default": "",
        "_special": "select_provider",
        "restart_required": true
      },
      "spam_threshold": {
        "description": "刷屏检测阈值",
        "type": "int",
        "default": 6
      },
      "spam_window_seconds": {
        "description": "刷屏检测窗口（秒）",
        "type": "int",
        "default": 60
      },
      "silent_hours_start": {
        "description": "静默时段开始（小时）",
        "type": "int",
        "default": 0
      },
      "silent_hours_end": {
        "description": "静默时段结束（小时）",
        "type": "int",
        "default": 6
      },
      "interest_sample_size": {
        "description": "兴趣关键词数量",
        "type": "int",
        "default": 20
      }
    }
  },
  "MetaThinking_Bot1": {
    "description": "对话规则 · Bot 1",
    "hint": "仅对 Bot 1 生效的覆盖配置",
    "type": "object",
    "items": {
      "qq_id": {
        "description": "QQ 号",
        "type": "string",
        "default": "2500447291",
        "restart_required": true
      },
      "name": {
        "description": "显示名称",
        "type": "string",
        "default": "羽书",
        "restart_required": true
      },
      "db_id": {
        "description": "数据库标识（用于 user_profiles/beliefs/concerns 等表，不是 QQ 号）",
        "hint": "必须保持为稳定短 ID；羽书默认 yushu。",
        "type": "string",
        "default": "yushu",
        "restart_required": true
      },
      "aliases": {
        "description": "别名（逗号分隔，用于兴趣词匹配和纠正检测）",
        "type": "string",
        "default": "羽书,羽书bot,器灵"
      },
      "exclude_sources": {
        "description": "排除的记忆来源（逗号分隔）",
        "hint": "羽书默认不直接注入白真真第一人称经历，避免身份串线。",
        "type": "string",
        "default": "bzz_experience,bzz_pending",
        "restart_required": true
      },
      "interest_keywords": {
        "description": "自定义兴趣关键词（逗号分隔，触发主动对话）",
        "type": "string",
        "default": "没钱修什么仙,张羽,熊狼狗"
      },
      "meta_prompt": {
        "description": "自定义 MetaThinking prompt（留空用默认模板）",
        "type": "text",
        "default": "",
        "restart_required": true
      },
      "model": {
        "description": "模型（留空用全局默认）",
        "type": "string",
        "default": "",
        "_special": "select_provider",
        "restart_required": true
      },
      "proactive_enabled": {
        "description": "启用主动对话",
        "type": "bool",
        "default": true
      },
      "proactive_interval_seconds": {
        "description": "主动对话最小间隔（秒）",
        "type": "int",
        "default": 600
      },
      "proactive_max_per_hour": {
        "description": "每小时最多主动插话",
        "type": "int",
        "default": 3
      }
    }
  },
  "MetaThinking_Bot2": {
    "description": "对话规则 · Bot 2",
    "hint": "仅对 Bot 2 生效的覆盖配置",
    "type": "object",
    "items": {
      "qq_id": {
        "description": "QQ 号（留空则不启用该 bot）",
        "type": "string",
        "default": "1336495069",
        "restart_required": true
      },
      "name": {
        "description": "显示名称",
        "type": "string",
        "default": "白真真",
        "restart_required": true
      },
      "db_id": {
        "description": "数据库标识（用于 user_profiles/beliefs/concerns 等表，不是 QQ 号）",
        "hint": "白真真默认 baizz，需与历史数据一致。",
        "type": "string",
        "default": "baizz",
        "restart_required": true
      },
      "aliases": {
        "description": "别名（逗号分隔）",
        "type": "string",
        "default": "白真真,阿真,白主管"
      },
      "exclude_sources": {
        "description": "排除的记忆来源（逗号分隔，留空表示不排除任何来源）",
        "type": "string",
        "default": "",
        "restart_required": true
      },
      "interest_keywords": {
        "description": "自定义兴趣关键词（逗号分隔）",
        "type": "string",
        "default": "白真真,张羽,安监局,灵币,极情剑道"
      },
      "meta_prompt": {
        "description": "自定义 MetaThinking prompt（留空用默认模板）",
        "type": "text",
        "default": "",
        "restart_required": true
      },
      "model": {
        "description": "模型（留空用全局默认）",
        "type": "string",
        "default": "",
        "_special": "select_provider",
        "restart_required": true
      },
      "proactive_enabled": {
        "description": "启用主动对话",
        "type": "bool",
        "default": false
      },
      "proactive_interval_seconds": {
        "description": "主动对话最小间隔（秒）",
        "type": "int",
        "default": 900
      },
      "proactive_max_per_hour": {
        "description": "每小时最多主动插话",
        "type": "int",
        "default": 2
      }
    }
  },
  "Eviction_Settings": {
    "description": "记忆淘汰",
    "hint": "记忆过多时按什么规则降级或清理",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用记忆淘汰",
        "type": "bool",
        "default": true
      },
      "noise_ttl_days": {
        "description": "噪声记忆保留天数",
        "type": "int",
        "default": 7
      },
      "interval_hours": {
        "description": "淘汰作业检查间隔（小时）",
        "type": "float",
        "default": 6.0
      }
    }
  },
  "Jargon_Settings": {
    "description": "黑话与口癖",
    "hint": "群内黑话的发现与使用",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用黑话挖掘与注入",
        "type": "bool",
        "default": true
      },
      "min_frequency": {
        "description": "黑话候选词频门限",
        "type": "int",
        "default": 5
      },
      "min_messages": {
        "description": "黑话候选消息数门限",
        "type": "int",
        "default": 10
      },
      "confidence_threshold": {
        "description": "推断置信度门限",
        "type": "float",
        "default": 0.5
      },
      "max_inject": {
        "description": "单次回复最大注入黑话数",
        "type": "int",
        "default": 3
      }
    }
  },
  "FewShot_Settings": {
    "description": "风格示范",
    "hint": "用已审核的回复范例影响说话风格",
    "type": "object",
    "items": {
      "enabled": {
        "description": "启用 FewShot 风格注入",
        "type": "bool",
        "default": true
      },
      "max_inject": {
        "description": "单次回复最大注入样例数",
        "type": "int",
        "default": 3
      }
    }
  },
  "Study_Settings": {
    "description": "自主学习",
    "hint": "沉淀对话经验的开关与频率",
    "type": "object",
    "items": {
      "self_reflect_enabled": {
        "description": "启用自省纠错系统",
        "type": "bool",
        "default": true
      }
    }
  },
  "Social_Settings": {
    "description": "社交与人际交往策略",
    "hint": "防骚扰阈值、冷却机制、社交能量沉淀上限与跨群权重参数。",
    "type": "object",
    "items": {
      "abuse_trigger_count": {
        "description": "防骚扰触发阈值",
        "hint": "群友连续进行人身攻击或恶意挑衅达到此次数后，Bot 将进入冷却静默状态（默认 3 次）。",
        "type": "int",
        "default": 3,
        "min": 1,
        "max": 10
      },
      "abuse_cooldown_base": {
        "description": "防骚扰冷却基础时长（秒）",
        "hint": "初次触发防骚扰静默的基础时长（默认 120 秒）。",
        "type": "int",
        "default": 120,
        "min": 10,
        "max": 3600
      },
      "abuse_cooldown_max": {
        "description": "防骚扰冷却最大时长（秒）",
        "hint": "多次被激怒后冷却时长翻倍的最大上限（默认 3600 秒）。",
        "type": "int",
        "default": 3600,
        "min": 60,
        "max": 86400
      },
      "aba_window_seconds": {
        "description": "连续对话判定时间窗（秒）",
        "hint": "群友在该时间窗内连续说话会被判定为同一轮互动会话（默认 30 秒）。",
        "type": "int",
        "default": 30,
        "min": 10,
        "max": 300
      },
      "group_weight_current": {
        "description": "当前群记忆匹配权重",
        "hint": "检索记忆时当前所在群的优先加权比例（默认 1.0）。",
        "type": "float",
        "default": 1.0,
        "min": 0.1,
        "max": 5.0
      },
      "group_weight_cross": {
        "description": "跨群记忆匹配权重",
        "hint": "跨群检索共享记忆时的折减权重比例（默认 0.7）。",
        "type": "float",
        "default": 0.7,
        "min": 0.1,
        "max": 2.0
      },
      "impact_cap": {
        "description": "单次交互印象冲击度上限",
        "hint": "单次对话产生的 impression 冲击度最大上限（默认 5.0）。",
        "type": "float",
        "default": 5.0,
        "min": 1.0,
        "max": 10.0
      },
      "affinity_step_cap": {
        "description": "阶段性好感单次调整步长上限",
        "hint": "未结算能量满触发结算时，好感单次增减的最大幅度限制（默认 2.0 分）。",
        "type": "float",
        "default": 2.0,
        "min": 0.5,
        "max": 10.0
      },
      "affinity_hostility_step_cap": {
        "description": "阶段性敌意单次调整步长上限",
        "hint": "未结算能量满触发结算时，敌意单次增减的最大幅度限制（默认 3.0 分）。",
        "type": "float",
        "default": 3.0,
        "min": 0.5,
        "max": 10.0
      }
    }
  },
  "Performance_Settings": {
    "description": "底层性能与批处理调优",
    "hint": "向量化批次尺寸与异步写入落盘缓冲间隔。",
    "type": "object",
    "items": {
      "embedding_batch_size": {
        "description": "向量化批处理大小",
        "hint": "单次调用 Embedding API 向量化记忆的最大批次量（默认 16）。",
        "type": "int",
        "default": 16,
        "min": 1,
        "max": 128
      },
      "write_flush_interval": {
        "description": "数据库写入落盘缓冲间隔（秒）",
        "hint": "高并发写入时批次协调器的落盘缓冲区间隔（默认 1.0 秒）。",
        "type": "float",
        "default": 1.0,
        "min": 0.1,
        "max": 10.0
      }
    }
  },
  "BookLore_Settings": {
    "description": "书设知识库存储路径",
    "hint": "只读语料数据库的外部挂载路径配置。",
    "type": "object",
    "items": {
      "book_lore_db_path": {
        "description": "书设只读数据库绝对或相对路径",
        "hint": "留空时自动在插件数据目录中查找 book_lore.db。",
        "type": "string",
        "default": ""
      },
      "lore_db_path": {
        "description": "书设别名数据库路径（兼容）",
        "hint": "留空默认沿用 book_lore_db_path。",
        "type": "string",
        "default": ""
      }
    }
  },
  "Channel_Settings": {
    "description": "注入通道底层配置",
    "hint": "各通道运行优先级与参数，通常在独立页面管理。",
    "type": "object",
    "items": {
      "layers": {
        "description": "注入通道优先级图层",
        "hint": "各注入通道的自定义优先级序列，通常在控制台通道配置页调整。",
        "type": "string",
        "default": ""
      }
    }
  },
  "Affinity_Settings": {
    "description": "好感度计算与手动校准边界",
    "hint": "关系系统计算边界与人工干预单次上限。",
    "type": "object",
    "items": {
      "manual_adjustment_delta_cap": {
        "description": "管理员手动调整好感单次上限",
        "hint": "管理台手动校准好感时的单次最大变动幅度（默认 20.0 分）。",
        "type": "float",
        "default": 20.0,
        "min": 1.0,
        "max": 50.0
      }
    }
  },
  "TagWorker_Settings": {
    "description": "标签异步处理工兵",
    "hint": "后台标签提取队列的工作机制。",
    "type": "object",
    "items": {
      "worker_enabled": {
        "description": "启用后台标签队列",
        "hint": "是否开启后台常驻协程处理记忆标签提取与分类（默认开启）。",
        "type": "bool",
        "default": true
      }
    }
  }
}